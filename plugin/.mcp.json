{
  "mcpServers": {
    "fireauto-mem": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/mem/mcp-server.cjs"],
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules",
        "DB_PATH": "${CLAUDE_PLUGIN_DATA}/fireauto-mem.db"
      }
    }
  }
}
